{
  "ex-accuracy": {
    "starterCode": "import torch\n\npredictions = torch.tensor([0, 1, 1, 0, 2])\ntargets = torch.tensor([0, 1, 0, 1, 2])\n\n# Calcule accuracy (proporção de acertos)\naccuracy = \n\nprint(f'Accuracy: {accuracy}')",
    "hints": [
      "Compare predictions == targets",
      "Converta para float e calcule a média",
      "3 acertos em 5: 0.6"
    ],
    "validation": {
      "type": "assert",
      "tests": [
        "acc_val = accuracy.item() if hasattr(accuracy, 'item') else accuracy",
        "assert abs(acc_val - 0.6) < 1e-5, f'Accuracy incorreta: esperado 0.6, obtido {acc_val}'"
      ]
    },
    "solution": "accuracy = (predictions == targets).float().mean()"
  }
}
