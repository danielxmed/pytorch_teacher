{
  "ex-conv-output": {
    "starterCode": "import torch\nimport torch.nn as nn\n\n# Crie Conv2d com kernel_size=5, stride=2, padding=0\nconv = \n\n# Input (1, 3, 64, 64)\nx = torch.randn(1, 3, 64, 64)\noutput = conv(x)\n\nprint(f'Output shape: {output.shape}')\n\n# Calcule o output shape esperado\n# Formula: (W - K + 2P) / S + 1 = (64 - 5 + 0) / 2 + 1 = 30\nexpected_size = ",
    "hints": [
      "Conv2d(in_channels=3, out_channels=16, kernel_size=5, stride=2, padding=0)",
      "Output size = (64 - 5 + 0) / 2 + 1 = 59/2 + 1 = 29.5 + 1 → floor = 30"
    ],
    "validation": {
      "type": "assert",
      "tests": [
        "assert output.shape[2] == 30, f'Height incorreto: esperado 30, obtido {output.shape[2]}'",
        "assert output.shape[3] == 30, f'Width incorreto: esperado 30, obtido {output.shape[3]}'"
      ]
    },
    "solution": "conv = nn.Conv2d(3, 16, kernel_size=5, stride=2, padding=0)\nexpected_size = 30"
  },
  "ex-mlp": {
    "starterCode": "import torch\nimport torch.nn as nn\n\n# Crie o MLP: Linear(784, 256) -> ReLU -> Dropout(0.2) -> Linear(256, 10)\nmlp = nn.Sequential(\n    \n)\n\n# Teste\nx = torch.randn(32, 784)\noutput = mlp(x)\nprint(f'Output shape: {output.shape}')",
    "hints": [
      "nn.Linear(784, 256)",
      "nn.ReLU()",
      "nn.Dropout(0.2)",
      "nn.Linear(256, 10)"
    ],
    "validation": {
      "type": "assert",
      "tests": [
        "x_test = torch.randn(16, 784)",
        "out = mlp(x_test)",
        "assert out.shape == torch.Size([16, 10]), f'Output shape incorreto: esperado (16, 10), obtido {tuple(out.shape)}'"
      ]
    },
    "solution": "mlp = nn.Sequential(\n    nn.Linear(784, 256),\n    nn.ReLU(),\n    nn.Dropout(0.2),\n    nn.Linear(256, 10)\n)"
  }
}
